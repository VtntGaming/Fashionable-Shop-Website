$kind: http-request
name: Get Personalized Recommendations
method: GET
url: '{{baseUrl}}/api/recommendations/personalized'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
