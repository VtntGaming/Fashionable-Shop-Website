$kind: http-request
name: Check Wishlist Item
method: GET
url: '{{baseUrl}}/api/wishlists/check/{{productId}}'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
