$kind: http-request
name: Update Order Status
method: PUT
url: '{{baseUrl}}/api/orders/{{orderId}}/status'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
queryParams:
  - key: status
    value: CONFIRMED
