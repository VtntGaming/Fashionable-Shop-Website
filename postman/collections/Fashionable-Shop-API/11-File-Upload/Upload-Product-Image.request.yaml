$kind: http-request
name: Upload Product Image
method: POST
url: '{{baseUrl}}/api/files/products/images'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: formdata
  content:
    - key: file
      type: file
      src: ''
