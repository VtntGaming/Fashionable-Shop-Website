$kind: http-request
name: Upload Product Images Batch
method: POST
url: '{{baseUrl}}/api/files/products/images/batch'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
body:
  type: formdata
  content:
    - key: files
      type: file
      src: ''
